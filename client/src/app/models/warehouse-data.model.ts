export interface WarehouseData {
    // just testing the data not the final work
    // also we need to boards and wheels and trucks 
    id?: number;
    location: string;
    boards: number;
    wheels:number;
    trucks: number;
}
